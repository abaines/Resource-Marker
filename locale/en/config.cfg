mod-setting-name.resourcemarker-generate-adjacent-chunks=Generate chunks adjacent to chunks with resources (default=true)
mod-setting-name.resourcemarker-chart-resource-chunks=Chart chunks (reveal map) with resources (default=true)
mod-setting-name.resourcemarker-minimum-size=Minimum resources to tag (default=1000)
mod-setting-name.resourcemarker-starting-radius-to-generate=Starting chunk radius to generate (default=20)

